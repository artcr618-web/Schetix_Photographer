<!-- ============================================================
     БЛОК «Сколько стоит привести одного клиента»
     ВЫНЕСЕН ИЗ ОТЧЁТА 09.08.2026 по решению заказчика.

     Причина: это про маркетинг, а не про юнит-экономику.
     Кроме того, в расчёте остались нерешённые вопросы:
       1. Часть продвижения работает на удержание старых клиентов,
          а не только на привлечение новых. Доля не определена.
       2. Переговоры считаются только по состоявшимся заказам.
          Время на тех, кто написал и пропал, нигде не учтено.
       3. Коэффициент 0,7 для постоянного клиента («переговоры
          короче на 30%») взят БЕЗ ИСТОЧНИКА. Обсуждалась цифра 50%.
       Для пунктов 2 и 3 нужен вопрос в анкете: из скольких
       обращений получается один заказ.

     Файл — хранилище, не рабочая страница. Чтобы вернуть блок,
     перенесите части ниже в части/каркас.html и увеличьте
     ожидаемое число блоков в собрать.py (19 → 20).

     ОСТАЛОСЬ В КАРКАСЕ (нужно блоку 03, не удалять):
       переменные ставкаЧ, поискЧ, перепЧ, реклЗ, новЧ, новР, постЧ, постР
     ============================================================ -->

ФРАГМЕНТЫ ДЛЯ ВОЗВРАТА В КАРКАС

=== 1. СТИЛИ (в <style> каркаса) ===
/* ==== БЛОК 07 · стоимость привлечения клиента ====
   Диаграмма — тот же приём, что в блоке 01: строки .lgi с заливкой
   по величине, группы .lggrp, итог отдельной строкой. Сравнение
   нового и постоянного — таблицей, чтобы не дублировать суммы. */
/* итог — крупная плашка с заголовком и двумя значениями внутри */
#phr-root .cacsum{margin-top:22px;border-radius:18px;padding:22px 24px;
box-shadow:0 6px 18px rgba(16,24,40,.16);
background:linear-gradient(140deg,#7C8698,#4E5768)}
#phr-root .cacsum .csh{font-size:15.5px;font-weight:700;color:var(--c-white);
letter-spacing:-.02em}
#phr-root .cacsum .csv{font-size:38px;font-weight:800;color:var(--c-white);
letter-spacing:-.035em;line-height:1.1;margin-top:8px;font-variant-numeric:tabular-nums}
#phr-root .cacsum .css{font-size:12.5px;line-height:1.55;color:rgba(255,255,255,.80);
margin:10px 0 18px}
#phr-root .cacsum .csg{display:grid;grid-template-columns:1fr 1fr;gap:12px}
#phr-root .cacsum .csg div{border-radius:14px;padding:15px 17px;
background:rgba(255,255,255,.13)}
#phr-root .cacsum .csg span{display:block;font-size:11.5px;
color:rgba(255,255,255,.78);margin-bottom:6px}
#phr-root .cacsum .csg b{display:block;font-size:24px;font-weight:800;
color:var(--c-white);letter-spacing:-.03em;font-variant-numeric:tabular-nums}
@media(max-width:700px){#phr-root .cacsum .csg{grid-template-columns:1fr}}
/* ссылка на статью справочника вместо пересказа состава */
#phr-root .lgref{color:var(--c-b-600);text-decoration:none;
border-bottom:1px dashed var(--c-b-250);font-size:12px}
#phr-root .lgref:hover{color:var(--c-b-800);border-bottom-color:var(--c-b-500)}
#phr-root .lgp{color:var(--c-gr2);font-size:12px;margin-right:8px}
#phr-root .lgp:after{content:'\00b7';margin-left:8px;color:var(--c-ln)}
/* ---- сравнение нового и постоянного ----
   Сверху две плашки с итогом, ниже заголовок и таблица состава. */
#phr-root .cmpw{margin-top:34px}
/* итоговые плашки: цена заявки для каждого типа клиента */
#phr-root .cmpg{display:grid;grid-template-columns:1fr 1fr;gap:12px}
#phr-root .cmpg div{border-radius:16px;padding:18px 20px;color:var(--c-white);
box-shadow:0 6px 18px rgba(16,24,40,.16)}
#phr-root .cmpg .gn{background:linear-gradient(140deg,var(--c-r-400),var(--c-r-600))}
#phr-root .cmpg .gp{background:linear-gradient(140deg,var(--c-g-400),var(--c-g-600))}
#phr-root .cmpg span{display:block;font-size:11.5px;letter-spacing:.05em;
text-transform:uppercase;color:rgba(255,255,255,.80);margin-bottom:8px}
#phr-root .cmpg b{display:block;font-size:30px;font-weight:800;letter-spacing:-.035em;
line-height:1.05;font-variant-numeric:tabular-nums}
#phr-root .cmpg em{display:block;font-style:normal;font-size:12px;margin-top:7px;
color:rgba(255,255,255,.85);line-height:1.45}
@media(max-width:700px){#phr-root .cmpg{grid-template-columns:1fr}}
/* заголовок и пояснение — ПОД плашками, перед таблицей */
#phr-root .cmph{font-size:12px;font-weight:700;letter-spacing:.07em;
text-transform:uppercase;color:var(--c-gr2);margin:30px 0 4px}
#phr-root .cmpn{font-size:13px;line-height:1.6;color:var(--c-gr);
margin-bottom:12px;text-align:left}
/* Таблица состава. Шапка без заливки — только подписи столбцов с чертой:
   серая плашка во всю ширину выглядела тяжелее самой таблицы. */
#phr-root .cmp{display:grid;grid-template-columns:1fr auto auto;
border:1px solid var(--c-ln);border-radius:16px;overflow:hidden}
#phr-root .cmp>div{padding:11px 16px;font-size:13.5px;color:var(--c-ink2);
display:flex;align-items:center;border-bottom:1px solid var(--c-ln2)}
#phr-root .cmp>div:nth-child(3n+2),#phr-root .cmp>div:nth-child(3n){
justify-content:flex-end;text-align:right;white-space:nowrap;min-width:118px;
font-variant-numeric:tabular-nums}
#phr-root .cmp>div.ch{font-size:11px;font-weight:700;letter-spacing:.05em;
text-transform:uppercase;color:var(--c-gr2);white-space:nowrap;
padding-top:12px;padding-bottom:8px;border-bottom:1px solid var(--c-ln)}
/* имена .cn/.cp заняты информационными плашками отчёта (min-height:210px
   и круг-псевдоэлемент) — здесь свои, с приставкой cmp */
#phr-root .cmp>div.ch.cmpn1{color:var(--c-r-700)}
#phr-root .cmp>div.ch.cmpp1{color:var(--c-g-800)}
#phr-root .cmp>div b{font-weight:700;color:var(--c-ink)}
#phr-root .cmp>div.tt{background:var(--c-surf);border-bottom:0}
#phr-root .cmp>div.tt b{font-size:15px;letter-spacing:-.02em}
#phr-root .cmp>div.tt.win b{color:var(--c-g-800)}
@media(max-width:700px){#phr-root .cmp{font-size:12.5px}
#phr-root .cmp>div{padding:10px 12px}
#phr-root .cmp>div:nth-child(3n+2),#phr-root .cmp>div:nth-child(3n){min-width:84px}}

=== 2. РАЗМЕТКА (перед блоком «Четыре цифры») ===
<div class="card" id="card11"><div class="bn">07</div>
<h2 class="big-h" data-t="rh2_22">Сколько стоит привести одного клиента</h2>
<p class="hint ctr" data-t="rhi_22">Заявка не берётся из воздуха: её оплачивает ваше время и ваш рекламный бюджет</p>
<div class="lgd" id="cacLg"></div>
<div id="cacCmp"></div>
<div class="ex" style="--exb:var(--c-b-300);--exg:var(--c-b-50);margin-top:24px" data-t="rgn_15">
<b>Почему это важно считать</b>Стоимость заявки — это то, во что обходится каждый новый заказчик до того, как он заплатит вам хоть рубль. Она складывается из вашего времени на поиск и переговоры и из денег, потраченных на рекламу и сайт.<br>Пока вы её не знаете, невозможно понять, окупается ли реклама и сколько вы на самом деле зарабатываете с заказа.</div>
</div>

=== 3. РАСЧЁТ (перед «четыре цифры») ===
  /* --- стоимость привлечения клиента (блок 07) ---
     Диаграмма строится тем же приёмом, что легенда блока 01: строки .lgi,
     ширина заливки — доля от наибольшей величины, группы .lggrp.
     Слагаемые разделены на две природы, как «время / деньги» в блоке 01. */
  var clg=$('cacLg');
  if(clg){
    var C_T1='#3B82F6', C_T2='#8B5CF6', C_D1='#E8A05C', C_D2='#B6BCC4';
    /* время специалиста оценивается по стоимости его часа */
    /* Под названием — не пересказ состава, а ссылка на статью справочника:
       определение живёт в одном месте и не расходится по блокам. */
    var сост=[
      {g:1,c:C_T1,t:'Продвижение и реклама',ref:'Продвижение и реклама',
       v:поискЧ*ставкаЧ,h:поискЧ},
      {g:1,c:C_T2,t:'Работа с клиентом',ref:'Работа с клиентом',
       v:перепЧ*ставкаЧ,h:перепЧ},
      {g:2,c:C_D1,t:'Продвижение и реклама',ref:'Продвижение и реклама',
       v:d.py>0?(d.varAds||0)/d.py:0},
      {g:2,c:C_D2,t:'Сайт',ref:'Сайт',пометка:'часть продвижения',
       v:d.py>0?(d.depSite||0)/d.py:0}];
    var мкс=0; сост.forEach(function(x){if(x.v>мкс)мкс=x.v});
    var якорь=window.PHR_REF||function(){return null};
    function строка(x){
      var w=мкс>0?x.v/мкс*100:0, сп=якорь(x.ref);
      return '<div class="lgi">'+
        '<div class="fill" style="width:'+w.toFixed(1)+'%;background:'+x.c+'"></div>'+
        '<div class="ltx"><i style="background:'+x.c+'"></i><div><b>'+x.t+'</b>'+
        '<em>'+(x.пометка?'<span class="lgp">'+x.пометка+'</span>':'')+
        (сп?'<a class="refl" href="#'+сп.id+'">Подробнее в п. '+parseInt(сп.n,10)+' Справочника</a>':'')+'</em></div></div>'+
        '<div class="lvv"><div class="a">'+f0(x.v)+'</div>'+
        (x.h!==undefined?'<div class="b2">за '+f1(x.h)+' '+hword(x.h)+'</div>':'')+
        '</div></div>';
    }
    var g1=сост.filter(function(x){return x.g===1});
    var g2=сост.filter(function(x){return x.g===2});
    clg.innerHTML=
      '<div class="lggrp">Ваше время</div>'+
      '<div class="note">Время, которые вы тратите на поиск заказа до того, как он оплачен. В денежном эквиваленте, исходя из стоимости часа вашей работы.</div>'+
      g1.map(строка).join('')+
      '<div class="lggrp gap">Прямые расходы</div>'+
      '<div class="note">Деньги, которые уходят на привлечение независимо от того, придёт клиент или нет</div>'+
      g2.map(строка).join('')+
      '<div class="cacsum">'+
        '<div class="csh">Столько стоит привести одного клиента</div>'+
        '<div class="csv">'+f0(новР)+'</div>'+
        '<div class="css">Ваше время переведено в деньги по стоимости часа вашей работы, поэтому вся сумма указана в рублях</div>'+
        '<div class="csg">'+
          '<div><span>Ваше время в деньгах</span><b>'+f0(новЧ*ставкаЧ)+'</b></div>'+
          '<div><span>Прямые расходы</span><b>'+f0(реклЗ)+'</b></div>'+
        '</div>'+
      '</div>';
  }
  /* --- сравнение нового и постоянного (блок 07) ---
     Порядок: сначала итог плашками, ниже заголовок и таблица состава.
     Заголовок объясняет, ЗАЧЕМ смотреть на сравнение, а не пересказывает
     выводы — их человек видит в цифрах сам. */
  var ccm=$('cacCmp');
  if(ccm){
    function ряд(им,нов,пост,кл){
      return '<div'+(кл?' class="'+кл+'"':'')+'>'+им+'</div>'+
             '<div'+(кл?' class="'+кл+'"':'')+'>'+нов+'</div>'+
             '<div'+(кл?' class="'+кл+' win"':'')+'>'+пост+'</div>';
    }
    ccm.innerHTML=
    '<div class="cmpw">'+
      '<div class="cmpg">'+
        '<div class="gn"><span>Новый клиент</span><b>'+f0(новР)+'</b>'+
          '<em>стоимость одной заявки</em></div>'+
        '<div class="gp"><span>Постоянный клиент</span><b>'+f0(постР)+'</b>'+
          '<em>дешевле в '+(постР>0?f1(новР/постР):'—')+' раза</em></div>'+
      '</div>'+
      '<div class="cmph">Новый клиент vs Постоянный</div>'+
      '<div class="cmpn">Сравните, во что обходится каждая заявка, — чтобы решать, сколько вкладывать в привлечение, а сколько в удержание тех, кто уже у вас покупал.</div>'+
      '<div class="cmp">'+
        '<div class="ch">Из чего складывается</div><div class="ch cmpn1">Новый</div><div class="ch cmpp1">Постоянный</div>'+
        ряд('Продвижение и реклама · ваше время',f1(поискЧ)+' ч','—')+
        ряд('Работа с клиентом · ваше время',f1(перепЧ)+' ч',f1(постЧ)+' ч')+
        ряд('Продвижение и реклама · расходы',f0(d.py>0?(d.varAds||0)/d.py:0),'—')+
        ряд('Сайт · часть продвижения',f0(d.py>0?(d.depSite||0)/d.py:0),'—')+
        ряд('<b>Эквивалент вашего времени в рублях</b>','<b>'+f0(новЧ*ставкаЧ)+'</b>',
            '<b>'+f0(постЧ*ставкаЧ)+'</b>','tt')+
      '</div>'+
    '</div>';
  }

=== 4. РАЗДЕЛ ПОЛНОЙ ТАБЛИЦЫ ===
    /* --- привлечение клиента --- */
    /* Цифры те же, что в блоке 03: считаются один раз выше по файлу. */
    rows.push(tgrp('\u041f\u0440\u0438\u0432\u043b\u0435\u0447\u0435\u043d\u0438\u0435 \u043a\u043b\u0438\u0435\u043d\u0442\u0430'));
    rows.push(trow('\u041f\u043e\u0438\u0441\u043a \u043a\u043b\u0438\u0435\u043d\u0442\u0430 \u0438 \u043f\u0440\u043e\u0434\u0432\u0438\u0436\u0435\u043d\u0438\u0435',hh(поискЧ),'\u043d\u0430 \u043e\u0434\u0438\u043d \u0437\u0430\u043a\u0430\u0437'));
    rows.push(trow('\u041f\u0435\u0440\u0435\u043f\u0438\u0441\u043a\u0430 \u0438 \u0441\u043e\u0433\u043b\u0430\u0441\u043e\u0432\u0430\u043d\u0438\u044f',hh(перепЧ),'\u043d\u0430 \u043e\u0434\u0438\u043d \u0437\u0430\u043a\u0430\u0437'));
    rows.push(trow('\u0420\u0435\u043a\u043b\u0430\u043c\u0430 \u0438 \u0441\u0430\u0439\u0442',f0(реклЗ),'\u043d\u0430 \u043e\u0434\u0438\u043d \u0437\u0430\u043a\u0430\u0437'));
    rows.push(trow('<b>\u0421\u0442\u043e\u0438\u0442 \u043f\u0440\u0438\u0432\u043b\u0435\u0447\u044c \u043d\u043e\u0432\u043e\u0433\u043e \u043a\u043b\u0438\u0435\u043d\u0442\u0430</b>','<b>'+f0(новР)+'</b>',hh(новЧ)+' \u0432\u0430\u0448\u0435\u0433\u043e \u0432\u0440\u0435\u043c\u0435\u043d\u0438'));
    rows.push(trow('\u0421\u0442\u043e\u0438\u0442 \u043f\u043e\u0432\u0442\u043e\u0440\u043d\u044b\u0439 \u0437\u0430\u043a\u0430\u0437 \u043f\u043e\u0441\u0442\u043e\u044f\u043d\u043d\u043e\u0433\u043e',f0(постР),hh(постЧ)+' \u0432\u0430\u0448\u0435\u0433\u043e \u0432\u0440\u0435\u043c\u0435\u043d\u0438'));
    if(постР>0)rows.push(trow('\u041f\u043e\u0441\u0442\u043e\u044f\u043d\u043d\u044b\u0439 \u0434\u0435\u0448\u0435\u0432\u043b\u0435 \u043d\u043e\u0432\u043e\u0433\u043e',f1(новР/постР)+' \u0440\u0430\u0437\u0430','\u044d\u043a\u043e\u043d\u043e\u043c\u0438\u044f \u043d\u0430 \u0437\u0430\u043a\u0430\u0437\u0435 '+f0(новР-постР)));

=== 5. СТРОКА «Сэкономлено на привлечении» ===
      rows.push(trow('\u0421\u044d\u043a\u043e\u043d\u043e\u043c\u043b\u0435\u043d\u043e \u043d\u0430 \u043f\u0440\u0438\u0432\u043b\u0435\u0447\u0435\u043d\u0438\u0438',f0(Math.round(постЗак*новР-постЗак*постР)),
        Math.round(постЗак*(новЧ-постЧ))+' \u0447 \u0432\u0430\u0448\u0435\u0433\u043e \u0432\u0440\u0435\u043c\u0435\u043d\u0438'));
