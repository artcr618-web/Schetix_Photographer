# З-106 · Таблица режимов блока 04 в стиле детализации
Приоритет: P1
Статус: ● внесено ПОВТОРНО 27.08 (корень поломки найден и устранён),
ждёт визуальной приёмки владельца.
Создана: 27.08.2026
Обновлена: 27.08.2026
Файлы: Веб/Части/каркас.html (источник), Веб/report.html (сборка)

## Зачем

Слово владельца (27.08, ночь): таблицу сравнения режимов налогообложения
блока 04 переоформить в точности как таблицы «Подробной детализации» —
«табличную разметку взять из раздела „Подробная детализация" — любую
таблицу», «количество столбцов и строк — из оригинальной таблицы блока 04,
все данные заводить оттуда», из детализации — только стиль оформления,
«в точности», «таблицы полностью, включая там закреплённые ячейки».

## Что сделать

- [x] Найти эталон в детализации: .wrap + table.rt + colgroup, шапка
      th.ш-имя/th.ш-знач, светлая строка заголовков, тонкие линии строк.
- [x] HTML блока 04: .txbox/.txw/table.tx/.txfoot → .wrap + table.rt
      + colgroup (col.cn + 13×col.cv); заголовки th.ш-имя/th.ш-знач;
      все data-t сохранены (14 шт.).
- [x] CSS: кожа .rt обобщена с #спрдет на #phr-root (единый стиль всех
      таблиц отчёта); #спрдет-специфика (td:first-child, nth-child(2)/(3),
      td.код) осталась на #спрдет.
- [x] CSS #card07: --w-имя:250px/--w-знач:116px, min-width по этим
      переменным; sticky первый столбец (td.имя и thead th.ш-имя) с белой
      подложкой и тонкой линией :after; tr.best — мягкая зелёная
      подложка var(--c-g-50) и галочка перед названием; tr.over —
      приглушённо; td.ok #15803D / td.no #B91C1C; подтексты обязанностей
      td i — 11.5px var(--c-gr2).
- [x] Удалено: весь CSS .tx (~70 строк), .txbox/.txw/.txfoot, кастомный
      скроллбар (бегунок/стрелки/углы), .tx в @media 820px, td.mk i,
      мёртвый JS-блок шкалы тонировки (шкала/тон/тонС/тон2).
- [x] JS рисоватьТаблицу: без зебры, без тонировки имён по выгоде,
      без классов num/ld/tot; ячОбяз → td.ok|no; строка «не подходит» —
      td.нз под строкой over; СОРТ/НАПРАВЛ и порядок «недоступные внизу»
      сохранены.
- [x] Мобильный блок 620px: sticky снят, --w-имя:142px/--w-знач:104px
      (общие с #спрдет), скроллбар скрыт (как у детализации — свайп).
- [x] 700px: кегль таблицы 12.5px — как у прочих .rt.
- [x] Печать: как у таблиц детализации — overflow:visible, min-width:0,
      кегль 10.5px, шапка переносится, sticky снят.
- [x] собрать.py (prev10 заархивирован) + проверить.py: 121 · 0 · 1
      (замечание прежнее — записи справочника без ссылок).

## Решения и правки

- 27.08.2026 (вечер) — по слову владельца СНЯТО ЗАКРЕПЛЕНИЕ первого
  столбца: «первый столбец должен также прокручиваться со всеми
  остальными, потому что на мобильном телефоне смотреть совершенно
  неудобно». Удалены: position:sticky у td.имя и thead th.ш-имя, белая
  подложка и линия :after у закреплённого края, правило «static» в
  мобильном и печатном блоках, а также отдельный border-collapse:
  separate (нужен был только ради рамок липкой ячейки — теперь таблица
  на общем collapse, как у всех .rt). Название режима осталось
  полужирным с переносом по словам. Проверено: сборка ✓, 121·0·1,
  jsdom-прогон — 5 строк, segtip есть, ошибок 0, card07-правил со
  sticky в сборке — ноль.
- 27.08.2026 (ещё позже) — ПОВТОРНОЕ ВНЕСЕНИЕ. Корень найден: при переносе
  кода через терминал из вызова `ячОбяз(o.касса)` потерялась буква —
  стало `o.касс`; ключа «касс» в ОБЯЗ нет → undefined → TypeError в
  ячОбяз → render() умирал → белая диаграмма и никаких подсказок.
  Исправлено; построчная сверка рисоватьТаблицу с prev10 подтвердила:
  отличия теперь ровно задуманные (без зебры/тонировки/num-классов,
  td.имя, td.нз, ok/no), случайных искажений нет.
  Проверки: собрать ✓, проверить 121·0·1, node --check ✓, полный прогон
  в jsdom: таблица 04 — 5 строк (как в prev10 на тех же данных), best 1,
  ok/no 23/13, segtip есть, 12 таблиц .rt в отчёте, ошибок 0.
- 27.08.2026 (позже) — ОТКАТ ПРЕДЪЯВЛЕНИЯ. Владелец: «всё сломалось,
  диаграмма белая, подсказок нет». Диагноз подтверждён прогоном в jsdom:
  `TypeError: Cannot read properties of undefined (reading '0')` в
  ячОбяз, вызов из рисоватьТаблицу → render() умирал → всё ниже по
  скрипту (кольцо, segtip, свайп-подсказки) не выполнялось.
  Действия: Веб/report.html восстановлен из Архив/Вёрстка/report_prev10.html
  (байт-в-байт, З-104 ит.5 + З-105 на месте). Прогон prev10 в jsdom:
  таблица 5 строк, segtip есть, ошибок 0 — версия рабочая.
- 27.08.2026 — задача заведена и сразу внесена.
- Разметка взята у строитель T() детализации: свайп-подсказка стоит
  перед таблицей (скрипт свайпа находит .wrap без правок — проверено,
  обёртка() ищет следующий sibling .wrap/.txbox, скроллер '.txw,.wrap').
- Классы srt и data-sort со шапки СНЯТЫ: поиск обработчика по всему
  файлу (data-sort/НАПРАВЛ/desc/srt, строки >5040) показал, что клика
  к сортировке подключено не было — стрелки и «активная» стрелка
  «Нагрузка» были мёртвой декорацией. НАПРАВЛ/СОРТ остались в JS:
  они задают исходный порядок строк (по нагрузке, недоступные внизу).
- Сортировка при collapse теряет рамки липкой ячейки, поэтому таблице
  блока 04 оставлен border-collapse:separate при border-spacing:0
  (как было в .tx) — вид тот же, что у прочих .rt.
- ПОДСВЕТКА «ВАШ РЕЖИМ»: раньше — мягкий синий var(--c-fns-4).
  Стиль детализации подсветки строк не предусматривает, но метка
  «ваш режим» — данные, без неё строку не найти. Переведена в мягкий
  зелёный var(--c-g-50) с галочкой var(--c-g-700) — под новую светлую
  кожу. ЕСЛИ ВЛАДЕЛЬЦУ НЕ НРАВИТСЯ — вернуть один символ: фон на
  var(--c-fns-4) (правило tr.best td, ~строка 1140).
- Свайп-подсказка и поведение прокрутки не тронуты; txNote («С чего
  считается налог») и bestbox/НЮАНС не тронуты.
- report.html: 629 → 624 КБ (минус мёртвый CSS/JS).

## Проверка

- собрать.py: теги/CSS/палитра/20 блоков/JS/логотип — ✓.
- проверить.py: 121 прошло · 0 упало · 1 замечание (прежнее).
- node --check всех 5 JS-блоков — ✓.
- colgroup: 1 cn + 13 cv = 14 столбцов = 14 th ✓; data-t блока 04 —
  все 14 на месте.
- Визуальная приёмка — за владельцем (в песочнице нет браузера):
  десктоп (sticky столбец, best/over, ok/no), 620px (sticky снят,
  свайп), печать.
- 27.08.2026 — Внесено: таблица блока 04 переоформлена в стиль детализации (.wrap+table.rt, colgroup, ш-имя/ш-знач); sticky первого столбца сохранён; srt/data-sort сняты как мёртвая декорация; шкала тонировки и мёртвый CSS удалены; сборка 121·0·1. Ждёт визуальной приёмки.
- 27.08.2026 — ОТКАТ предъявления: в сборке З-106 рантайм-ошибка (ячОбяз, п undefined) убивала render() — белая диаграмма, нет подсказок. report.html восстановлен из prev10 (проверено в jsdom: 0 ошибок). Наработки остались в каркасе, не в сборке. Ждёт решения владельца.
- 27.08.2026 — Повторное внесение: корень найден — потерянная буква в o.касса (транспорт съел «а»), из-за неё умирал весь render(). Исправлено, сверено с prev10 построчно, прогон в jsdom без ошибок (5 строк, segtip есть). Ждёт визуальной приёмки.
- 27.08.2026 — Снято закрепление первого столбца по слову владельца: столбец прокручивается вместе со всеми (на телефоне заморозка мешала). Удалены sticky-правила td.имя/th.ш-имя, подложка, линия :after, static-правила (620px, печать), border-collapse:separate. Проверено: 121·0·1, jsdom 0 ошибок, sticky-правил card07 в сборке нет.
