# З-016 · PDF основного отчёта
Приоритет: P0
Статус: ▶ в работе
Создана: 18.08.2026
Обновлена: 30.08.2026
Файлы: Веб/report.html, Веб/Части/каркас.html, Инструменты/создать_pdf_отчёта.py, PDF/

## Зачем

Подготовить воспроизводимые PDF основного отчёта, справочника и подробной детализации с корректной A4-вёрсткой и динамическими данными Счётикса.

## Что сделать

- [x] Три режима печати и автоматическая сборка Chromium
- [x] Колонтитулы, дата, нумерация и автоматическое имя файла
- [x] Автоматические данные справочника и вводных детализации
- [x] Горизонтальный A4 и таблицы в пределах листа
- [ ] Завершить визуальные правки по замечаниям владельца

## Решения и правки

- 30.08.2026 — печатные таблицы окончательно упрощены: скруглений нет, внешние левая и правая границы удалены, вертикальных разделителей нет; остаются только горизонтальные строки. Разделительные линии колонтитулов снова имеют одинаковую безопасную отбивку 4 мм на всех страницах.
- 30.08.2026 — выполнена публикационная уборка: `package.json` и `package-lock.json` перенесены в `Инструменты`, проверка автоматически устанавливает jsdom оттуда; удалены восемь неиспользуемых инструментов прежней сборки report и три противоречащих текущей архитектуре документа. З-016 восстановлена как корректная активная задача, реестр пересобран. Краткосрочный архив очищен по пятидневному окну без удаления исходных изображений и контрольной книги 27.08; сборщик передачи теперь оставляет только последний датированный ZIP и `АКТУАЛЬНАЯ.zip`.
- 30.08.2026 — устранена зависимость колонтитулов от браузерной опции «Фоновая графика». Верхняя шапка и полный логотип перенесены из CSS `background-image` в печатное `content: url(...)` настоящих margin boxes; разделительные линии — в обычные границы. Реальным Chromium через DevTools Protocol создан PDF с `printBackground:false`: верхний колонтитул, нумерация, нижний логотип и `schetiks.ru` присутствуют. Имя, предлагаемое браузером при сохранении, теперь формируется автоматически: `Расчёт от ДД.ММ.ГГГГ · Юнит-экономика для фотографов · Счётикс`. По решению владельца в детализации полностью удалены обобщающие рамки разделов: остаются номер, заголовок и обычная таблица. Исправлена нумерация подразделов «Износ камеры» с 06.1–06.3 на 07.1–07.3 в report, каркасе и источнике прототипа.
- 30.08.2026 — по замечанию владельца колонка «Код» в таблицах детализации выровнена по левому краю — и заголовок, и значения. Для каждого фрагмента длинного раздела добавлена отдельная нижняя внутренняя отбивка и усилена внешняя рамка: после строк 17, 37 и на остальных разрывах теперь видна самостоятельная округлённая граница именно раздела, а прямая внутренняя рамка таблицы не подменяет её. Визуально проверены окончания первых трёх страниц и начало второй страницы.
- 30.08.2026 — данные PDF переведены на воспроизводимую цепочку. Вводные детали уже строятся из `d.answers` и `d.catalog`, расчётные значения — из `d`; это зафиксировано отдельной архитектурой и сценариями в `Документация/АРХИТЕКТУРА_ДАННЫХ_PDF.md`. Справочник больше не берётся из вручную поддерживаемого словаря: `справочник_в_отчёт.py` читает строки `user` листа `01_Глоссарий` (плюс ранее утверждённое пользовательское meta-понятие «Значение по умолчанию») и включён первым шагом в `карта.py`. Новое утверждённое user-определение автоматически попадает в report и PDF при пересборке. Первая паспортная шапка переразложена внутри более полного макета: её заголовок теперь находится на той же высоте от верхнего края, что на страницах 2–11; доход и ставка стоят одной строкой, дата — следующей, всё находится выше разделительной линии.
- 30.08.2026 — владелец утвердила горизонтальный A4 для всех трёх PDF. Основной отчёт, справочник и отдельная детализация переведены в landscape; после пересборки они занимают соответственно 26, 20 и 11 страниц. Печатные ширины колонок детализации перераспределены внутри доступной ширины, пустой экранный хвост таблицы скрыт: колонка «Код» и остальные значения больше не выходят за правую рамку раздела, боковые отбивки одинаковы. При переносе длинной таблицы скругляется только внешняя граница раздела; внутренняя рамка таблицы имеет прямые углы и визуально продолжается на следующем листе. Все 11 страниц отдельной детализации проверены визуально.
- 30.08.2026 — по замечанию владельца доработаны продолжения PDF. На каждой странице сохранена дата расчёта и добавлена нумерация в формате `текущая / всего`. У фирменного блока название «счётикс» и подпись «твоя юнит-экономика» приведены к одной точной ширине через SVG `textLength`, без нарушения соотношения с четырёхромбовым знаком. Разделительные линии верхнего и нижнего колонтитулов сдвинуты внутрь полей страницы, чтобы создать одинаковые безопасные отбивки до таблиц. Между соседними таблицами детализации увеличен интервал; при разрыве рамка и скругления клонируются на каждом печатном фрагменте. Визуально проверены верх, низ и все 5 страниц детализации.
- 30.08.2026 — по замечанию владельца полный паспортный колонтитул с Желаемым доходом, «Ставкой часа» и датой оставлен только на первой странице каждого PDF через `@page:first`. На последующих страницах верхний колонтитул сокращён до названия документа и поясняющего подзаголовка. Пересобраны и визуально проверены все 5 страниц отдельной детализации: первая страница полная, страницы 2–5 сокращённые, перекрытий нет.
- 30.08.2026 — по замечанию владельца паспортная часть PDF собрана единым левым блоком: название режима, пояснение, Желаемый доход, «Ставка часа» и дата расчёта. «Желаемая ставка» оставлена внутренним расчётным понятием, а в пользовательском колонтитуле используется утверждённая понятная подпись «Ставка часа», соответствующая родовому понятию «Ставка часа съёмки (стоимость часа съёмки)» из глоссария. Нижний колонтитул дополнен полным фирменным блоком: четырёхромбовый знак, «счётикс», подпись «твоя юнит-экономика» и `schetiks.ru`. Пересобраны и визуально проверены все 5 страниц отдельной детализации: границы и отсутствие перекрытий сохранены.
- 30.08.2026 — по замечанию владельца колонтитулы перенесены из fixed-HTML в настоящие margin boxes `@page`: они физически ограничивают область таблиц и не перекрывают контент. Верх: название режима, «Расчёт юнит-экономики фотографа», Желаемый доход, Желаемая ставка, дата. Низ: точный четырёхромбовый знак Счётикса, название и `schetiks.ru`. Визуально проверена первая страница детализации; повтор заголовка убран.
- 30.08.2026 — владелец отложила публикационные подключения до завершения дизайна и подачи calc/report/PDF. Для верхнего колонтитула утверждены: название режима, «Расчёт юнит-экономики фотографа», Желаемый доход, Желаемая ставка и дата. Для нижнего: фирменный четырёхромбовый знак, «счётикс», `schetiks.ru`.
- 30.08.2026 — fixed-HTML признан непригодным для многостраничных таблиц Chromium: элементы попадали внутрь потока. Реализованы настоящие CSS margin boxes `@top-left`, `@top-right`, `@bottom-left`, `@bottom-right`; поля A4 физически отделяют контент. Визуально проверены все 5 страниц детализации: перекрытий нет, колонтитулы повторяются, логотип и адрес находятся внизу.
