# Отложенные вопросы — вернуться после завершения report.html

Заведён 8 сентября 2026 по решению владельца: работаем по текущему сценарию
(доводка `report.html`, стадия линейных диаграмм под кольцом), а накопившиеся
вопросы складываем сюда. Разбираем **после** сверки — часть может закрыться
сама по ходу правок.

Правила: ничего из перечисленного не трогаю без отдельной команды.

---

## 1. `camWear` — два числа под одним смыслом · ВЕРОЯТНО УЖЕ РЕШЁН

**Состояние на 08.09: похоже, закрыт вашим решением от 04.09, но не везде.**

В новом блоке 13 «Сколько стоит ваш кадр ДЛЯ ВАС?» стоит комментарий:
«Затвор — от ВСЕГО съёмочного оборудования (решение владельца 04.09):
каждый щелчок задействует весь съёмочный комплект».

| Место | Формула | Число |
|---|---|---|
| блок 13, `report.html:7202` | `d.depShoot / d.shotsYear` | **1,42 ₽** |
| детализация, `report.html:6226` | `d.camWear` = `камАм / shotsYear` | **0,73 ₽** |
| книга, `camera_wear_model` | доля выработки | 0,2527 (%/год) |

Подпись у обеих строк отчёта одинаковая по смыслу — «за одно срабатывание».
Пользователь видит два разных числа.

**Вопрос:** привести детализацию к базе 04.09 (`depShoot/shotsYear`) или у
строки детализации свой смысл (только камера) и её надо переподписать?

---

## 2. Домен: `schetix.ru` или `schetiks.ru`

Расхождение сохраняется и слегка изменилось после обновления.

| Написание | Где |
|---|---|
| `schetiks.ru` | `calc.html`, `price.html`, `report.html`, `Окна/прайс_печать.js`, `Части/каркас.html`, сторож `проверить.py:900` |
| `schetix.ru` | `report.html`, `Счётчики/счётчик_сердечек.js`, `Части/окно_поделиться.html` |

`report.html` содержит **оба**. Воркер сердечек разрешает CORS только
`schetix.ru` и `calc.schetix.ru` — при рабочем `schetiks.ru` счётчик молча
перестанет засчитывать.

Точка продолжения от 05.09 говорит: «Поделиться» на всех страницах делится
ссылкой `https://schetiks.ru` — то есть вариант **с «s»** выглядит рабочим,
но воркер не обновлён.

**Вопрос:** подтвердить `schetiks.ru` и привести к нему воркер и оставшиеся
места? (Относится к З-012, которая ещё не начата.)

---

## 3. `price.html` вне контура книги

Страница активно развивается (задача 4, шаги 1–2 выполнены), но:

- на листах `Интерфейс` и `Тексты` — **0 упоминаний**;
- в `CSS_и_компоненты` её собственный CSS (677 правил, 2 617 деклараций)
  не разобран;
- `проверить.py` её не проверяет, `демо.py` не обновляет;
- 21 поле без программной подписи;
- ID блоков нерегулярны: `PRICE-B001`, `B0A0`, `B002`, `B00R`, `B003`, `B004`,
  `B0S1`, `B005`…`B007`, `B00T`, `B009`, `B008`.

В точке продолжения от 05.09 отмечено: «прайс на листе по-прежнему не
отражён — инструмент `собрать_интерфейс.py` читает только index/calc/report».
То есть вы об этом знаете.

**Вопрос:** вводить прайс в контур после завершения задачи 4 или он остаётся
отдельным?

---

## 4. Мёртвые CSS-переменные `--exb` / `--exg`

Объявляются в `report.html` прямо в атрибутах `style` (5 вхождений, было 3),
задают цвета через `var(--c-g-300)`, `var(--c-p-300)`. **Не читаются нигде** —
правила `.ex`, которое бы их использовало, в CSS нет.

Похоже на задуманные, но не доведённые цветные рамки блоков-примеров.

**Вопрос:** доделать цветные рамки или убрать объявления?

---

## 5. Молчащие предохранители

`calc.html:3242`:

```javascript
var ПРЕДЕЛ_СТОРОННЕГО = NT*0.8;
var side = promo + accT;
if(side > ПРЕДЕЛ_СТОРОННЕГО){
  var к = ПРЕДЕЛ_СТОРОННЕГО/side;
  promo *= к; accT *= к;      // ← ужимает молча
}
```

Пользователь вводит, например, 8 ч/день на продвижение, значения
пропорционально ужимаются, **никакого уведомления не показывается** —
человек не понимает, почему цифры не сходятся с введёнными.

Так же молча работают зажимы процентов (10 / 20 / 15 %) и `D ≥ 0,40`.

**Вопрос:** показывать предупреждение, как это уже сделано для полей
с немедленными пределами (решение З-035 от 28.08)?

---

## 6. Мелкое, на потом

- `CRED = 3000` в `calc.html` объявлена и не используется ни разу;
- `pool` присваивается в контракт `d` дважды;
- `work_months = 11` помечен «не участвует», хотя `hour_all` его использует;
- жёсткая ссылка `#спр-77` в блоке точки безубыточности ведёт на «Срок службы»;
  нумерация `REFD` автогенерируемая, ссылка тестами не покрыта;
- 120 правил `calc.html` и 52 `report.html` объявлены без корневого префикса
  `#phX-root` — при встраивании в Tilda возможны утечки стилей;
- витрина `Части/уведомления.html` велит «править сначала здесь», но отстала
  от продукта на два цвета: `--c-g-200` (`#CFE9D6` против `#D8EEDF`) и
  `--c-a-500` (`#E0A200` против `#D9762E`).

---

## 7. Состояние проверок (фон, не трогаем по решению владельца)

```text
238 прошло · 7 упало · 1 замечание
```

Из семи падений:

- **2** — следствие намеренно пустого `Архив/` (RULE-010, RULE-018 ссылаются
  на архивные документы; отсутствует ZIP передачи). Владелец подтвердил
  08.09: архив пуст сознательно, места в аренде нет;
- **5** — ожидаемый фон незавершённой работы над report: сторож ждёт 20 блоков,
  сейчас 28; ждёт старую нумерацию 01–08, сейчас сквозная 01–15; headless
  падает на том же счёте блоков.

После завершения report сторожа нужно будет обновить под новую структуру.

## Добавлено 08.09 (после восстановления вёрстки report.html)

- **Рассинхрон `Веб/Части/каркас.html`.** Каркас отстал от `report.html`:
  20 блоков против 29. Правки B006/B027/B029, канон шапок 01/02/03, функция
  `чм()` и текст группы ролей внесены **только в `report.html`**.
  Вопрос владельцу: каркас — действующая витрина или архивная копия?
  Если действующая — нужна отдельная задача на синхронизацию.
- **Ключи `rpp_01` / `rpp_02`** (блок «Маржинальная прибыль», REPORT-B029) отсутствуют на листе «Тексты» книги. Внести через `карта.py`.
- **5 битых якорей `#thanks`** в report.html — присутствуют и в базовой версии
  коммита, к правкам этой сессии отношения не имеют. Раздел «Поблагодарить»
  в отчёте отсутствует: удалить ссылки или добавить раздел?
- **`idle` против `sh`** (219 против 216,6 ч) — множитель 1,0111. Расчёт верен,
  но смысловое решение за владельцем: приравнивать ли неэффективное время
  съёмочному.
