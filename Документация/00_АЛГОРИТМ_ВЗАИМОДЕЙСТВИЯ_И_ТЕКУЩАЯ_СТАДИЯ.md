# Счётикс — алгоритм взаимодействия и текущая стадия

Обновлено: 15 сентября 2026.

## 1. Роли

### Владелец

Владелец определяет:

- смысл понятий;
- пользовательские названия и определения;
- состав показателей;
- методику расчёта;
- пользовательскую подачу и дизайн;
- приоритет работ.

Голосовые формулировки могут содержать оговорки. Ассистент понимает намерение,
но не принимает продуктовые решения вместо владельца.

### Ассистент

Ассистент:

- устанавливает фактические связи до правки;
- находит источник, а не первое текстовое совпадение;
- прослеживает последствия на несколько шагов;
- меняет только утверждённое;
- синхронизирует все обязательные поверхности;
- запускает проверки после каждой правки;
- фиксирует находки и следующую задачу.

Название продукта пишется **Счётикс**, с прописной и через «ё».

## 2. Как выполняется одна задача

1. Установить, что меняется: буква, понятие, формула, состояние или дизайн.
2. Найти источник правды.
3. Построить цепочку влияния.
4. Остановиться, если требуется смысловое решение владельца.
5. Зафиксировать состояние до правки.
6. Изменить минимально необходимое.
7. Провести изменение через зеркала.
8. Пересобрать автоматические листы и книгу, если требуется.
9. Запустить полную проверку.
10. Сообщить, что сделано и что сознательно не изменялось.
11. Обязательно назвать следующую задачу и её место в дорожной карте.

Нельзя завершать действие сообщением «сделано» без направления дальше. Если
нужно решение — показывается один конкретный вопрос. Если решение не нужно —
ассистент продолжает техническую работу самостоятельно.

## 3. Источники правды

| Сущность | Источник |
|---|---|
| Исполняемый расчёт | `calc()` в `Веб/calc.html` |
| Понятия и определения | `01_Глоссарий` основной книги |
| Пользовательский справочник | строки `user` листа `01_Глоссарий`; автоматически публикуются командой `карта.py` |
| Формулы книги | `calc` |
| Состав групп | `Состав` |
| Значения и состояния анкеты | `Значения_по_умолчанию` |
| Страницы, блоки, элементы и ветки | `Интерфейс` |
| Пользовательские показатели и контракт `d` | `Полный_отчёт` |
| Программа лояльности | `Программа_лояльности` и фактический код report |
| Правила | `Архитектура — Правила модели и интерфейса.md` |
| Задачи | `Задачи/З-*.md` и `Задачи/РЕЕСТР.md` |

Определения глоссария предназначены пользователю. Формулы, ID, внутренние ветки
и порядок промежуточных действий хранятся в `calc` и паспортах.

## 4. Обязательная цепочка влияния

```text
понятие
→ поле анкеты
→ calc()
→ поле d
→ report
→ основная книга
→ паспорт
→ правило
→ тест
```

Для новой вёрстки используются:

- `data-page-id`;
- `data-block-id`;
- `data-branch-id`;
- DOM-ID;
- `data-t`;
- поля `d`;
- понятия глоссария;
- паспорта;
- `Интерфейс`.

Новая вёрстка меняет расположение и оформление, но не создаёт вторую логику.

## 5. Правила, которые нельзя нарушать

1. Не переименовывать понятие и не менять состав без решения владельца.
2. Любое пользовательское название сверять с `01_Глоссарий`.
3. Все понятия с назначением `user` дословно публиковать в справочнике.
4. Не добавлять в пользовательское определение формулы и внутренние ID.
5. Не улучшать соседний элемент по пути.
6. Не делать самостоятельный редизайн report до получения новой вёрстки.
7. Смысловые и расчётные изменения проводить через действующий HTML сразу.
8. Выключенный необязательный параметр остаётся в структуре и получает
   эффективный ноль.
9. Отрицательный пользовательский ввод отклоняется; вычисленный убыток
   сохраняется со знаком минус.
10. Деньги внутри модели хранятся точно; пользователю показываются до рубля.
11. Практическая ставка округляется вверх до 100 ₽ только в утверждённых местах.
12. Логистика не входит в базовый расчёт и учитывается для конкретного проекта.
13. Старая архивная книга и архивные документы не являются рабочими источниками.
14. После правки запускать `проверить.py`; значение «упало» всегда 0.
15. Найденная, но не исправленная проблема немедленно попадает в дорожную карту
    или отдельную задачу.
16. 13.09 (слово владельца): изменения, о которых не просили прямо, не
    выполняются — сначала совет с владельцем; если прямого разрешения
    нет, ничего не менять.

## 6. Основная книга

```text
Книга/Калькулятор_ставки_часа.xlsx
```

В ней ровно 11 листов:

1. `00_Читать`;
2. `01_Глоссарий`;
3. `calc`;
4. `Состав`;
5. `Значения_по_умолчанию`;
6. `Интерфейс`;
7. `Тексты`;
8. `CSS_и_компоненты`;
9. `Адаптивность` (добавлен 03.09.2026: контрольные точки, общие правила
   перестроения и исключения по блокам; параметры вносятся после утверждения,
   паспорт — `Документация/CSS/Адаптивность.md`);
10. `Программа_лояльности`;
11. `Полный_отчёт`.

Контрольное состояние:

```text
1 329 формул
101 верхнеуровневое поле d
15 сценариев Excel = calc()
0 ошибок формул
0 пустых кэшей
```

Старая книга сохранена только в:

```text
Архив/Книга/
Калькулятор_ставки_часа_контрольная_до_миграции_2026-08-27.xlsx
```

## 7. Повторяемые команды

```bash
cd /home/user/Schetix_Photographer

# Полная пересборка автоматически получаемых листов и пересчёт
python3 Инструменты/карта.py

# Интеграционная проверка
python3 Инструменты/проверить.py /home/user/Schetix_Photographer

# Глоссарий ↔ справочник ↔ Состав
python3 Инструменты/сверить_глоссарий.py

# 15 сценариев Excel ↔ настоящий calc()
python3 Инструменты/проверить_сценарии_чистой_книги.py
```

Норма:

```text
245 прошло · 0 упало · 1 информационное замечание
```

## 8. Текущая стадия

З-041 завершена:

- каждый старый лист проверен;
- основная книга пересобрана в 10 листов (03.09.2026 добавлен 11-й лист `Адаптивность`);
- формулы совместимы со старым Excel и LibreOffice;
- старая книга архивирована;
- генераторы и проверки переключены;
- З-041 закрыта и находится в `Архив/Задачи/`.

З-044 также завершена: README, этот регламент, АЛГОРИТМЫ и текущие разделы
дорожной карты синхронизированы.

З-045 закрыта: новая вёрстка report интегрирована, получила 20 технических
блоков, актуальный контракт, справочник, тексты и CSS. З-035 закрыта: все
числовые поля анкеты получили утверждённые правила. З-038 закрыта: единая архитектура эффективных нулей, веток и пользовательских ответов report завершена. З-026 закрыта: report проходит 8 постоянных jsdom-сценариев. З-016 активна повторно: владелец продолжает доработку дизайна и подачи PDF. Публикационные подключения отложены до завершения calc/report/PDF.

15.09, волна 15, блок 06 (REPORT-B008, `#card02`): идея диаграммы «из чего
складывается час съёмки» (кольцевые, 2D- и 3D-пирамиды) показана владельцу в
виде отдельных макетов — отклонена (3D-пирамида — «на два с минусом»,
«никуда негодная»); макетные файлы по указанию владельца удалены. Вечером
15.09 владелец сам выдал новую спецификацию, реализованную в `report.html`:

- легенда под календарём — две группы: «Оплачиваемое время: Съёмка» и
  «Скрытое время (не оплачивается напрямую):» + все остальные виды;
- плашка главной формулы `#wfpl`: две части по цветам — зелёная «1 час /
  съёмка — оплачивается» = серая «X часов / рабочее время»; X считается из
  `d` (сумма всех видов времени ÷ часы съёмки; демо: 1752 ÷ 216,6 = 8,1);
- таблица «куда уходит время» `#wtab`: строки — виды времени календаря,
  столбцы — за 1 час съёмки / неделю / месяц / год; разметка взята в точности
  из подробной детализации (`.свайп` + `.wrap` + `table.rt` с colgroup и
  классами шапки), включая мобильные трансформации (620/700) и печать;
- проверка свайпа — `wtSwip()` после `render();` (класс `.есть-прокрутка`).

Контракт чекера `проверить_report_headless.js`: таблиц 12 → 13 (15.09).
Headless: только 16 предсуществующих ✗ «не 20 блоков», новых ошибок нет.

15.09, волна 15, блок 06, 2-й круг (слово владельца, вечер):

- ₽ и белые прочерки — в КАЛЕНДАРЕ (в каждой занятой ячейке вместо
  подписи: съёмка → ₽, остальное → белый прочерк rgba(255,255,255,.9);
  title-подсказки сохранены; span абсолютно позиционирован (Z-118) —
  геометрия сетки не меняется; desktop и mobile — одна разметка).
  В ТАБЛИЦЕ — обратно наименования (первый столбец «Наименование»,
  без ₽/прочерков);
- плашка — ОДНА большая зелёная (эталонный градиент), формула «1 час
  съёмки = X часов рабочего времени», без деления и «=»;
- легенда — по левому краю, от начала ячеек таблицы;
- заголовок таблицы «На что уходит время»;
- восстановлена разделительная полоса после таблицы (1px var(--c-ln));
- расстояния ×3: календарь→плашка 54px, плашка→таблица 48px (mobile 40px).

15.09, волна 15, блок 06, 3-й круг (слово владельца, вечер):

- календарь: текстовые подписи вернулись на десктопе и планшете;
  символы (₽ / белый прочерк) — ТОЛЬКО там, где тексты пропадают
  (≤519px, Z-118): в каждой занятой ячейке два span (`.wtxt` текст,
  `.wmb` символ, оба абсолютно позиционированы — геометрия не
  меняется);
- разделительная линия — ПОСЛЕ КАЛЕНДАРЯ (не после таблицы), выделяет
  условные обозначения (28px / mobile 24px, 1px var(--c-ln));
- заголовки групп легенды — цветом текста самих обозначений
  (var(--gr) = #6B7280), а не чёрным;
- легенда — «по ячейкам таблицы»: от начала текста первой ячейки
  (рамка 1px + паддинг td 12px = 13px), а не по краю карточки.

15.09, вечер, блок 02 (B028) и блок 04 (B030) — 4-й круг (слово
владельца):

- B028: заголовок→баннер было 10px (крайне мало) → 36px (референс
  блок 12: 10px после h2 + 26px до контента): `.накоп .cnd.nak`
  margin-top 26px;
- B028: подпись на ДЕСКТОПЕ — тем же кеглем, что цифра (54px;
  mobile 27px/2 строки не тронута): в `@media(min-width:821px)` —
  `.tpw .tp{font-size:54px;line-height:1.15}`;
- B028: подпись второго баннера «На замену/ремонт техники» →
  «На замену техники»;
- B04 (мобильный ≤820): продажник сдвинут к плашке: перекрытие
  margin-right -0.1261H → -0.1525H (кромка плашки 0.6527W → 0.58W,
  проходит по середине плеча; белая полоса устранена; кисть до
  10,5px от кромки, текст с 16px — наездов нет). Обмер силуэта PNG:
  правый край плеча 0,63W. Десктоп (белая плашка) не тронут.

15.09, вечер, 5-й круг (слово владельца):

- B028: заголовок «За год работы вы накопите» → «За 1 год работы
  вы накопите» (просилось неоднократно);
- B028: десктопные баннеры — однострочный вариант с подписью 54px
  отменён («неудачно»): теперь ТАКАЯ ЖЕ структура, что мобильная —
  цифра 54px сверху, черта (длина = длина суммы), подпись ПОД
  чертой 30px (пропорционально мобильной 27:15 → 54:30), всё по
  левому краю, группа прижата к верху (@media(min-width:821px));
- Блок 06: формула — не «полоска», а баннер в языке баннеров
  документа: рецепт .rate-plq блока 11 (градиент g-700→g-500 +
  радиал 88% 0%, тень плашки, заголовок «Главная формула» 13px)
  + цифры в рамочках, как ряд .bbg блока 12 (рамка
  rgba(255,255,255,.22), радиус 14, подложка rgba(255,255,255,.07),
  число 34px сверху, подпись 13px снизу): [1 час / съёмка] =
  [8,1 часа / рабочее время]; mobile: рамочки flex:1, число 23px
  (≤380px — 20px).

15.09, вечер, 6-й круг (слово владельца, только B028-десктоп):

- «текст прижат к краям» → поля баннера 26/28 → 26/93: левый край
  текста выровнен по ПЕРВОЙ ЯЧЕЙКЕ календаря блока 06 ниже
  (28px поле карточки + 58px колонка дня + 7px зазор = 93px от
  общего края контента — баннер и карточка стоят с одного края),
  правое поле = левое;
- «На отпуск всё ещё крупный» → подпись 30px → 16px/opacity .9:
  эталон — главный баннер calc.html (.hero .big 54px + .hero .un
  16px);
- «отступ сверху × 1,5»: фактический зазор h2→баннер до правки
  был 26px, а не 36 (маржины 10 и 26 СХОЛПЫВАЮТСЯ до максимума —
  урок: зазоры между соседями мерить с учётом margin collapse):
  26 × 1,5 = 39px — явный: h2 margin-bottom:0 (мобильный зазор
  остаётся 26px), баннер margin-top:39px в @821;
- всё ТОЛЬКО @media(min-width:821px) — мобильная версия не тронута.

15.09, волна 15, блок 02 (REPORT-B028 «Что накопится при этой ставке»):
десктоп-баннеры — одна строка: подпись + сумма рядом (цифра 54px, как у
основного показателя обложки `.hero .big` в calc.html; мобильная
двухстрочная версия ≤820px утверждена и не тронута, цифра 27px). Фото
«отпуск» (закат/бесконечный бассейн, 1672×941) переправлено владельцем и
сохранено как `Веб/img/баннер_отпуск.jpg`; фото «техника» удалено по
указанию владельца — ожидает новое фото или команду убрать баннер.
Плашки блока 04: белая обводка в CSS отсутствует на всех ширинах
(border:0, проверено); владелец видит «белый бордюр» на мобильном —
уточняется по скриншоту.

Универсальный перенос между сессиями выполняется по комплекту
`Документация/Переезд_между_сессиями/` и не заменяет текущую дорожную карту.

## 9. Следующий крупный этап

Продолжить З-016 по одному замечанию владельца; после принятия PDF вернуться к оставшимся правкам calc/report, а публикационные подключения выполнять только по новой команде.

Порядок сведения:

1. сохранить утверждённую расчётную логику;
2. сопоставить страницы и блоки по стабильным ID;
3. сопоставить поля с контрактом `d`;
4. перенести пользовательские тексты;
5. обновить `Интерфейс`;
6. добавить report-тексты в `Тексты`;
7. добавить report-CSS в `CSS_и_компоненты`;
8. проверить мобильную и печатную версии;
9. выполнить публикационную проверку.

Параллельно не теряются отдельные задачи:

- З-035 — валидация полей;
- З-039 — публикационная копия без DEMO;
- З-042 — экономика постоянного клиента;
- З-043 — аналитика, UTM и цели;
- З-012/013/014 — домен, платежи и счётчик.

## 10. Исторические документы

`03_ПОЛНАЯ_МЕХАНИКА_И_АЛГОРИТМЫ_25авг_2026.md` и архивные аудиты нужны для
истории решений. В них есть старые листы, формулы, количества блоков и проверки.
Перед использованием любой их строки факт нужно заново подтвердить по текущему
HTML, основной книге и тестам.
