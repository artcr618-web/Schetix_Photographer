# ПЕРЕДАЧА В НОВУЮ СЕССИЮ · «Счётикс»

Составлено 19 августа 2026, в конце сессии. Читать первым, вместе с
`КАРТА_ПРОЕКТА.md` и `ДОРОЖНАЯ_КАРТА.md`.

Состояние на момент передачи: **63 прошло · 0 упало · 1 замечание**.
Всё, что описано ниже как «сделано», проверено запуском.

---

## 0. Как работать с этим человеком (правила, нарушать нельзя)

1. **Ничего не переименовывать и не менять по своей инициативе.** Согласованная
   формулировка, порядок строк, состав таблицы не трогаются. Кажется, что можно
   лучше — сначала предложить, дождаться «да», потом делать. Правило нарушалось
   трижды, каждый раз стоило двух ходов.
2. **Названия — только из справочника.** Нет понятия — сначала завести запись
   (определение пишет пользователь), потом использовать название.
3. **Отвечать по-русски.** Пользователь диктует голосом: текст сбивчивый,
   с опечатками. Понимать намерение, опечатки исправлять молча.
4. **Не спрашивать по мелочам** — делать и коротко отчитываться. По спорному —
   предлагать с обоснованием цифрами.
5. **Текстовых пояснений на страницы не добавлять** — только справочник и подсказки.
6. **Замечания по дизайну — в задачу З-029**, не править по ходу. Пользователь
   предупредила, что сама будет сбиваться на дизайн, — надо напоминать.
7. **Правки вносить во все связанные файлы сразу**, после каждой —
   `python3 ДОКУМЕНТАЦИЯ/4_инструменты/проверить.py schetix`, «упало» = 0.
8. В просмотр открывать тот файл, который правили.
9. Написание: **«Счётикс»**, с прописной и через «ё».
10. Пользователь ценит: честность про пробелы, признание своих ошибок,
    конкретные числа, отраслевые нормы со ссылками.

---

## 1. Что сделано в этой сессии

**Благодарность и платежи.** Нажатие кнопки «Поддержать» больше не засчитывает
поддержку: счётчик, метка `phc_thx_paid` и заголовок «Спасибо за поддержку!»
ставятся только при возврате с `?paid=1`. Поправлено в `calc.html`,
`report.html`, `paid.html`, `части/каркас.html`, `части/окно_благодарности.html`.
Заголовок при клике на сердечко решили не менять (данные полевых экспериментов
не подтверждают пользу «спасибо авансом»).

**Глоссарий сведён в один источник.** Лист `01_Глоссарий` книги — 21 раздел,
260 строк, 12 столбцов. `модель/глоссарий.html` — тот же лист для чтения глазами.
Скрипт читает лист **по заголовкам столбцов**, поэтому порядок столбцов можно
менять. В книгу пишет только с ключом `--в-таблицу`.

Разметка: № · Наименование · Статус _(врем.)_ · Справочник для сверки _(врем.)_ ·
ID · Тип · Определение · Определение утверждено · Описание алгоритма ·
Формула (пример расчёта) · Где используется · Решение _(врем.)_.

Статусы: **108 тождественно · 65 переименовать · 3 нет понятия · 84 не сверяется**.
Определений: **116 согласовано · 138 черновик**.

**Справочник — 77 записей.** Добавлены и внесены дословно: Юнит-экономика,
Значение по умолчанию, Операционное время, Работа с клиентами, Операционная
работа с клиентами, Проектная работа с клиентами. «Работа с клиентом»
переименована в «Проектная работа с клиентами» везде (цифры не менялись).
113 определений в книге заменены определениями справочника.

**Отчёт появился в книге** — лист `06b_Отчёт_полный`: 47 показателей, у 26 живые
формулы Excel, 24 сходятся с калькулятором, столбец «Сходится» показывает это
сам. 21 показатель без формул — этих параметров в книге нет (задача З-033).

**Убрано из глоссария как структура:** разделы A2 Разделы анкеты, A3 Блоки
анкеты, A4 Формы. Они дублировали листы `07_Интерфейс`, `08_UI`, `03_Каталоги`.
A1, A5, A6 оставлены по прямому указанию.

**Пути в генераторах** больше не зашиты: корень ищется вверх по дереву от файла
(по книге), папка инструментов — рядом или внутри. Поправлено в `демо.py`,
`таблицы_прототип.py`, `группировки.py`, `отчёт_в_книгу.py`.

**Новые инструменты:** `web/части/справочник_в_отчёт.py` (переносит справочник
в отчёт: `REFD`, `СПР`, карта `СВЯЗЬ` — раньше делалось руками и один массив
отстал на четыре записи), `модель/отчёт_в_книгу.py`,
`ДОКУМЕНТАЦИЯ/4_инструменты/сверить_глоссарий.py`.

**Удалены** промежуточные файлы `модель/сверка_названий.md`,
`модель/глоссарий_без_понятий.md`, снимок `модель/глоссарий_данные.json`
(был вторым источником правды).

---

## 2. Открытые вопросы — нужны решения пользователя

Заданы, ответа нет. Первым делом в новой сессии — получить их.

1. **Прототипы в `web/части/`.** Три разошлись с отчётом (`03.html`,
   `09_баннер_сценарии.html`, `предварительный_отчёт.html`) — предложено
   в `_архив`. `окно_благодарности.html` совпадает; `окно_поделиться.html`,
   `уведомления.html`, `уведомление_поделиться.html` — образцы окон;
   `07_привлечение_клиента.html` — хранилище изъятого блока с инструкцией
   возврата; `таблицы_прототип.html` — результат генератора.
   Вопрос: по факту расхождения или все девять в архив?
2. **Страховые взносы в детализации.** В `таблицы_прототип.py` строка
   захардкожена: `('Страховые взносы', 0)`. Для НПД верно, для УСН и АУСН
   57 390 ₽ лежат внутри `taxAll`, отдельного поля в `calc()` нет.
   Вопрос: отдельной строкой (правка `calc()`) или «Налоги и взносы» одной?
3. **Пять дублей CSS**, где нижнее правило молча перебивает верхнее:
   `.dn.big .mid .v` (27 → 46 → 30 px), `.drow` (flex → grid), `.drow .nm b`,
   `.drow .nm em`, `.wk .hr` в анкете.
   Вопрос: чинить сейчас, отдать в З-029, подключать ли сторож
   `_архив/скрипты/проверка_вёрстки.py` к `собрать.py`?
4. **Лист `04_Итог`, блок «Сколько вы теряете».** Формулы съехали: `B38` пусто,
   `B41 = B38−B37` даёт −0,027, `B43` пусто, `B44 = 0` вместо 685 729 ₽,
   у `B42` нет подписи. На `06_Отчёт` та же величина посчитана верно.
   Вопрос: восстановить по образцу или удалить блок, раз есть `06b`?
5. **Две задачи готовы к закрытию:** З-018 «Единые названия расходов»
   (фактически выполнена) и З-015 «Убрать временные файлы» (`web/paid.html`
   и `web/смайлик.svg` — ссылок на них нет).
   Вопрос: закрывать и удалять?
6. **«Значение по умолчанию»** в книге помечено внутренним (тип `meta`),
   но это запись справочника, видимая человеку. Менять признак или нет?
7. **«Минимальный выезд»**: в справочнике определён как стоимость, в анкете —
   часы. Не решено с прошлой сессии.
8. **Переименовывать ли** «Стоимость клиентского часа» → «Клиентская стоимость
   часа работы». Висит с прошлой сессии.

---

## 2а. Мои ответы на восемь вопросов (19 августа)

Это рекомендации с обоснованием. Окончательное слово за пользователем,
но если решения не будет — делать так.

**1. Битые пути.** Посылка неверна для этой машины: папка
`/home/user/ДОКУМЕНТАЦИЯ/4_инструменты` существует, все четыре генератора
запускались и отрабатывали. Но рекомендация верная по сути — **сделано
вторым способом**: корень ищется вверх по дереву от самого файла (по книге),
папка инструментов — рядом с корнем или внутри него. Переезд папок скрипты
больше не ломает.

**2. Прототипы.** Согласиться с разделением по факту расхождения:
в `_архив` три файла — `03.html`, `09_баннер_сценарии.html`,
`предварительный_отчёт.html`. Остальные шесть оставить: `окно_благодарности.html`
совпадает с отчётом, три файла окон — образцы, `07_привлечение_клиента.html` —
хранилище изъятого блока с инструкцией возврата, `таблицы_прототип.html` —
результат генератора. И завести правило: прототип, который разошёлся
с рабочим файлом, либо становится генератором, либо уезжает в архив.
Одинаковое решение для всех девяти хуже: потеряем то, что ещё работает.

**3. Страховые взносы — отдельной строкой.** Причины: в справочнике это
самостоятельное понятие («Страховые взносы» и «Налоги» — две записи),
на УСН и АУСН это 57 390 ₽ в год, и «0 ₽» будет прямой ложью в отчёте.
Значит нужно поле в `calc()` — правка источника, оформить задачей.
Слияние в «Налоги и взносы» противоречит нашему же правилу: одно название
на две сущности мы как раз и разгребаем.

**4. Дубли CSS — чинить сразу, это не дизайн.** Удаление перебитого верхнего
объявления не меняет вид ни на пиксель: страница уже работает по нижнему.
Это уборка мусора, а не правка оформления, поэтому в З-029 не место.
Сторож `_архив/скрипты/проверка_вёрстки.py` подключить к `собрать.py`
и к `проверить.py`, чтобы новые дубли ловились сами.

**5. Лист `04_Итог` — восстановить по образцу `06_Отчёт`.** Удалять рано:
`06b_Отчёт_полный` пока неполный, у 21 показателя формул нет. Восстановление
дешёвое — правильные формулы уже есть на `06_Отчёт`, строки 47 и 116.
Заодно дописать подпись в `A42`.

**6. Строка 46 листа `06b` — исправлено в этой сессии.** Ошибка была моя.
Теперь две строки: «Остаётся на руки при текущей ставке» 636 863 ₽
и «Недополученный доход» 685 729 ₽ = 1 322 592 − 636 863.

**7. Порядок — да, сначала технический блок.** Пути, `pip install formulas`,
подключение сторожа вёрстки, чистка `__pycache__`. Это минуты и нулевой риск,
зато дальше всё пересобирается одной командой. Потом идти по разделу 3.

**8. З-018 и З-015 — закрыть.** Названия расходов действительно единые,
ссылок на `web/paid.html` и `web/смайлик.svg` нет. С одной поправкой:
`paid.html` не удалять совсем, а перенести в `_архив/вёрстка` — сегодня
выяснилось, что в нём живёт копия окна благодарности, и он пригодится
как образец. `смайлик.svg` удалить.

**Дополнительно, из того же ряда:**
- «Значение по умолчанию» — считать видимым человеку (это запись справочника),
  снять признак внутреннего.
- «Минимальный выезд» — привести к одному: в справочнике стоимость,
  в анкете часы. Предлагаю оставить часы (так считает `calc()`),
  а определение поправить.
- «Стоимость клиентского часа» — не переименовывать: название согласовано
  и уже стоит в справочнике, отчёте и книге.

---

## 3. Порядок работ (согласован с пользователем)

| № | Что | Задача |
|---|---|---|
| 1 | Досверить справочник: 3 строки «нет понятия», 65 «переименовать» | — |
| 2 | Убрать временные столбцы, связать справочник с листом глоссария | — |
| 3 | Обновить `09f_Группы_затрат` — единая группировка | З-031 |
| 4 | Диаграммы на новых группах | З-011 |
| 5 | Достроить 21 показатель отчёта в книге | З-033 |
| 6 | Разбор отчёта блок за блоком | З-007 |
| 7 | Первый экран: расхождения из раздела 5 | З-009 |
| 8 | Дизайн — одним заходом в конце | З-029 |

Открытых задач 26, закрытых 7. Полный список — `ЗАДАЧИ/РЕЕСТР.md`.

**Этапы по глоссарию** (записаны в `ДОРОЖНАЯ_КАРТА.md`):
сейчас правим таблицу через временные столбцы → когда согласовано, столбцы
удаляем → справочник перестаёт хранить определения у себя и берёт их из листа
`01_Глоссарий`. С этого момента источник один на весь проект.

**Три строки «нет понятия»:** «Живу в своём» (`own_home`), «Всего расходов»
(`total_costs`), «ВСЕГО РАСХОДОВ (C)» — последние две одна величина,
записанная дважды.

---

## 4. Известные расхождения (не забыть)

1. **«Всего расходов» — два смысла**: 719 943 ₽ на обложке (с амортизацией
   и налогами) и 441 255 ₽ в таблице 03 (только прямые). Одно название.
2. **«Полная стоимость часа работы»**: в книге 1 332 ₽ (делит на 1 533),
   в расчёте 1 166 ₽ (делит на 1 752). Задача З-006.
3. **«Рабочее время»**: в книге 1 976 ч (247 × 8), в расчёте 1 752 ч (219 × 8).
4. **Ставка часа**: на обложке округлена до 9 500, в таблице 05 — 9 430.
5. **«Рабочие паузы» 219 ч** сидят в «Продвижение и прочее», отдельной строки
   в таблице 02 нет — надо вернуть.

---

## 5. Ключевые числа (значения анкеты по умолчанию)

```
ставка часа съёмки     9 430 ₽ (на обложке 9 500)
выручка                2 042 535 ₽/год · 170 211 ₽/мес
доход на руки          1 322 592 ₽/год · 110 216 ₽/мес
расходы дела           562 668 ₽ = амортизация 278 688 + текущие 283 980
налог НПД 5 %          102 127 ₽ · эквайринг 2,7 % = 55 148 ₽
всего затрат           719 943 ₽
точка безубыточности   609 608 ₽ · ставка в ноль 2 814 ₽
себестоимость часа     3 324 ₽ · наценка 183,7 %
остаётся при текущей ставке 636 863 ₽ · недополучено 685 729 ₽
время: рабочее 1 752 ч (219 × 8) · эффективное 1 533 ч (219 × 7) · проектное 1 083 ч
съёмка 216,6 · постпродакшн 649,8 · проектная работа с клиентами 216,6
поиск заказов 438 · учёт 12 · паузы 219 · форс-мажор 0
проектов 216,6 · время на проект 5 ч · кадров с часа 80
камера Canon EOS R6 Mark II 166 999 ₽ / 3 года → 55 666 ₽/год
затвор: 350 срабатываний/час · ресурс 300 000 · 75 810/год · выработка 73,4 %/год
```

**Три ставки часа (названия согласованы):**
- Фактическая стоимость часа работы = доход ÷ 1 752 = **755 ₽**
- Полная стоимость часа работы = выручка ÷ 1 752 = **1 166 ₽**
- Стоимость клиентского часа = выручка ÷ 1 083 = **1 886 ₽**

---

## 6. Команды

```bash
# проверка — после каждой правки, «упало» должно быть 0
python3 ДОКУМЕНТАЦИЯ/4_инструменты/проверить.py schetix

# глоссарий: собрать HTML из листа книги (в книгу не пишет)
python3 модель/глоссарий.py
# записать лист в единой разметке — только по прямой команде
python3 модель/глоссарий.py --в-таблицу

# перенести справочник в отчёт (REFD, СПР, СВЯЗЬ)
python3 web/части/справочник_в_отчёт.py
python3 web/части/подсказки.py          # подсказки в анкете
python3 web/части/демо.py               # демо-набор отчёта

# отчёт в книге и пересчёт формул
python3 модель/отчёт_в_книгу.py
pip install formulas                     # пакеты не переживают перезапуск
python3 ДОКУМЕНТАЦИЯ/4_инструменты/пересчитать_книгу.py

# значения настоящего calc() из командной строки
node ДОКУМЕНТАЦИЯ/4_инструменты/харнесс.js /home/user/schetix '{}'

# задачи
python3 ДОКУМЕНТАЦИЯ/4_инструменты/задачи.py список
python3 ДОКУМЕНТАЦИЯ/4_инструменты/задачи.py запись З-0NN "текст"

# сервер для просмотра
python3 -m http.server 3002 --bind 0.0.0.0 --directory /home/user/schetix/web
```

**Грабли:** `openpyxl` теряет диаграммы книги — писать листы только через zip
(так делают `глоссарий.py`, `отчёт_в_книгу.py`, `лист_в_книгу.py`).
В JS `\w` не ловит кириллицу. У `calc.html` нет тега `</body>` — скрипты
вставлять в конец файла. `formulas` и `jsdom` ставить заново после перезапуска.

---

## 7. Что почистить

- удалить `web/paid.html`, `web/смайлик.svg` (после ответа на вопрос 5);
- лишние резервные копии книги: `.до_глоссария`, `.до_отчёта`, `.до_пересчёта` —
  оставить одну;
- `__pycache__` в `модель/` и `web/части/`;
- прототипы в `web/части/` — по решению вопроса 1;
- устаревшие документы: `ЧИТАТЬ.txt` («рабочих файлов три»),
  `Первый_запуск/01_СПИСОК_ДОРАБОТОК.md` (дублирует систему задач),
  `Первый_запуск/04_РЕВИЗИЯ_ДОКУМЕНТАЦИИ.md` (закрыт, в архив),
  `Первый_запуск/05_РАЗБОР_ОТЧЁТА.md` (писался до переноса детализации),
  `ДОКУМЕНТАЦИЯ/1_как_устроено/01_Механика_расчёта.md` (старые названия времени);
- `модель/группировки.html` устарел частично: сделан до переименования работы
  с клиентами и до понятия «Операционное время».

Устаревшие листы книги: `09f_Группы_затрат` (З-031), `12_Диаграммы` (З-011),
`09b_Методика_цифр` (З-030), `09i_Справочник` (25 терминов старой редакции
плюс 19 пар «было → стало», противоречит справочнику), `06_Отчёт`
(доход по ролям от старой модели).

---

## 8. С чего начать новую сессию

1. Прочитать `КАРТА_ПРОЕКТА.md`, `ДОРОЖНАЯ_КАРТА.md`, этот файл.
2. `python3 ДОКУМЕНТАЦИЯ/4_инструменты/задачи.py список`.
3. `python3 ДОКУМЕНТАЦИЯ/4_инструменты/проверить.py schetix` — убедиться, что 0 упало.
4. Задать пользователю восемь вопросов из раздела 2 — коротко, списком.
5. Идти по порядку работ из раздела 3.
