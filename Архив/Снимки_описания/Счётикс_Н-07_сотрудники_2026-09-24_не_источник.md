# Н-07 · Наёмные сотрудники

**Статус:** ⏸ слово, после того как связка показа подтверждена.  
**Задание:** «Наёмные сотрудники: в ставке есть, отдельной строки отчёта нет.»

---

## 1. Факт, снятый 24.09 (HEAD 97bc09f)

### Анкета `calc.html`

- Вопрос 18, блок Form024, галочка `data-exc="Form024" checked` по умолчанию — сотрудники **выключены** по умолчанию (канон 24.09).
- Когда включено (галочка снята), `Form024` входит в переменные расходы и в `C` (общие расходы за год).

**Код:**
```js
// calc.html — Form024 входит в vari и C, но не в разбивку отчёта
// sumF('Form024') — в var* ?
```
Проверка `grep Form024 calc.html` — `data-add`, `data-clear`, `data-exc` — блок сотрудников с кнопкой «+ Добавить сотрудника».

Харнесс:
- `live.json` (Form024 checked) — `C=457 454,57`
- `base.json` (Form024 не checked, т.е. включен) — `C` больше? Проверим: в `base.json` Form024 включен, `C` = 457 454,57 тоже? В таблице расхождений `C` не было — значит Form024 в базе тоже выключен? Подожди, база `EXC={}` — значит Form024 включен (не исключён). Но `C` в base и live одинаков (457 454,57) — значит Form024 не влияет на `C` в дефолте, т.к. строк нет? Логично: по умолчанию сотрудников 0, поэтому `C` не меняется.

Прогон с сотрудником:
```js
// добавим сотрудника: 1 человек, 30 000 ₽/мес * 11 мес = 330 000 ₽/год в var?
```

### Отчёт `report.html`

- `grep -n Form024 report.html` — 0 вхождений.
- В `parts()` нет отдельной строки для сотрудников.
- В разбивке `dep*`/`var*` — `varAds`, `varSoft`, `varBank`, `varRent`, `varAcc` — сотрудников нет.
- Сумма строк разбивки может не сходиться с общей суммой `C` на величину сотрудников — зафиксировано в «Появилось по ходу» 23.09 как родитель Н-07.

**Доказательство из `probe_blocks_detail.json`:**
- `factGrid` не содержит «Сотрудники».
- `dnExp`, `lg2` — ваши расходы — не показывают сотрудников отдельно.

---

## 2. Вопрос владельцу

Оставить деньги сотрудников только внутри общей суммы `C` (как сейчас) или завести им свою строку отчёта «Сотрудники»?

- **Если оставить внутри общей:** сумма строк разбивки не сходится с `C` на величину сотрудников — нужно пояснение в тексте или в детализации.
- **Если завести строку:** добавить новую позицию в `parts()` (например, индекс 18) и в `visIdx`, и в кольцо бюджета (или в «Ваши расходы»), и в справочник.

Строку не добавляю, пока не сказано (правило 16).

---

## 3. Предложение для Н-07

**Предложение:** зафиксировать текущее поведение как «деньги внутри общей суммы» и добавить пояснение в блок «Ваши расходы» или в детализацию, что сотрудники учтены в общей сумме, но отдельной строкой не показываются — до решения о новой строке.

Если хотите отдельную строку — скажите «делаем Н-07 — заводим строку Сотрудники» — сделаю только её, без других правок.

---

*Файл — предложение по Н-07, не правка. Ждёт слова.*
