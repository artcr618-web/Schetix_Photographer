# Н-06 · Что из новых пунктов доходит до отчёта (обновление 24.09, канон управление выключено)

**Статус в дорожной карте:** ✔ съём 23.09, страницы не менялись. Прогон с включёнными галочками не делался.  
**Условие 24.09:** `calc.html` — эталон, управление по умолчанию **отключено** (`FormMgmt` checked), `Form024` checked, `fund_on` без checked. Прайс не трогаем.

Отчёт ставку заново не считает. Он показывает поля, которые отдал `calc()` через `PHR_D`.

---

## 1. Обновлённая таблица (24.09, живой дефолт)

| Пункт анкеты | Вопрос | В часе (calc) | Что видит отчёт (report) | Доказательство |
|---|---|---|---|---|
| **Управление делом** | 09, `FormMgmt` checked по умолчанию | **Нет** по умолчанию. Когда галочка снята (включено) — да, время `mgmtT` входит в пул `workHours=1752` и в `k=1+(idle+fmT)/эфф` не входит (свой сектор). | **Да, но с дефектами при 0.** `mgmtT` читается кольцом B030 (`monFig`), календарём B008 (`mgW`) и таблицей `wtBd` (должна, но сейчас не читает). При `mgmtT=0` плашка `monC4` остаётся с 0 ₽/мес 0.0% (Д1), кружок 0,0 (Д2), выноска NaN (Д3), легенда без управления (Д4), таблица недели без управления (Д5). При `mgmtT>0` всё рисуется корректно (5 секторов, 5 плашек, без NaN). | `live.json mgmtT=0`, `base.json mgmtT=87,6`, `probe.js monC4 рост=1.25`, `blocks_detail.json` |
| **Буфер ликвидности** | 21, `cushion_on` без checked по умолчанию | Да, если галочка включена. Доля прибавляется к доле прибыли и сидит внутри `fundY`. Отдельного `cushionY` анкета не отдаёт, только `fundY` и `fundP`. | Отчёт умеет отделить: `fundY − R × fundP` = `суммаБуфера(d)`, `суммаЧистойПрибыли(d)` = `R×fundP`. При снятой галочке оба 0. Включённый случай числами не снят (Н-09). | `calc.html:5394`, `report.html:5260 PHR_PROFIT_Y`, `report.html:9777-9810` |
| **Прибыльность дела (маржинальная прибыль)** | 22, `fund_on` без checked по умолчанию | Да, если галочка включена. | Да, через ту же пару `fundY`/`fundP`. При `fund_on` без checked — жёлтый баннер в `#mpZero` «Доля чистой прибыли не заложена / Ваше дело работает в ноль» (LIVE). При `fund_on` checked — баннер прибыли в `#mpHost` «Ваше дело принесёт чистую прибыль X ₽/год» (DEMO, если `fundY>0`). В живом дефолте `fundY=0`, поэтому DEMO и LIVE расходятся ещё и по `fund_on`, не только по управлению. | `live.json fundY=0`, `blocks_detail.json mpHost пусто в LIVE`, `report.html:9811-9870` |
| **Наёмные сотрудники** | 18, `Form024` checked по умолчанию, `Form024` — сотрудники | Да. Входят в переменные расходы `var*` и в `C`. `dep*` не трогают. | **Отдельной строки нет.** `Form024` в `report.html` не встречается (grep 0). Деньги внутри общей суммы `C=457 454,57`, в разбивке `dep*`/`var*` их поля нет. Сумма строк разбивки может не сходиться с общей суммой на величину сотрудников — зафиксировано в «Появилось по ходу» 23.09 как Н-07. | `calc.html data-exc="Form024" checked`, `grep Form024 report.html =0`, `live.json C` |
| **Обучение** | `Form006`, не исключён по умолчанию, `edu_life` | Сумма `eduY` считается и кладётся в `d.eduY`, в расходы часа не прибавляется. `depEdu` принудительно 0 (решение 15.09). | Строка «Амортизация обучения» читает этот 0. Сама `eduY` в показе не найдена (в `factGrid` нет). Ноль в амортизации верный (Н-08, слово владельца 23.09). | `calc.html:5365 eduEx`, `report.html: ~factGrid`, `live.json depEdu=0, eduY=74 409` |
| **Заказанный сайт (подрядчик)** | `Form014`/`Form010`, не исключён, `site_mode=contractor` | `siteY` считается и кладётся в `d`, в расходы часа не входит. `depSite` принудительно 0. | Строка «Амортизация сайта» читает 0. | `calc.html:5376 siteEx`, `live.json depSite=0` |
| **Свой сайт (самостоятельно)** | `site_mode=self`, `site_time` | Входит не деньгами, а долей времени в знаменателе ставки (`K`? нет, через `workHours`? фактически через `S`? — время на создание сайта). | Как компенсация времени, не как стоимость сайта. В отчёте показывается как «Сколько времени вы потратили на создание сайта» (если `site_self`), а не «Сколько вы заплатили». | `calc.html:5390`, `report.html: ~` |

---

## 2. Что изменилось по сравнению со съёмом 23.09

Старая таблица Н-06 писала: «Управление делом, вопрос 09 | Да. Время входит в пул.» — это верно для включённого состояния, но по умолчанию теперь **нет**. Обновлённая формулировка: «Нет по умолчанию (FormMgmt checked), да когда включено».

Остальные строки — без изменений, но с добавлением доказательств из живых прогонов 24.09.

---

## 3. Предложение для Н-06

**Предложение:** обновить строку Н-06 в дорожной карте на таблицу выше и считать Н-06 ✔ (обновлённый съём) без правок страниц.

- Управление — по умолчанию выключено, но когда включено — участвует в кольце, календаре и таблице (с дефектами Д1-Д5 при 0).
- Буфер и прибыль — по умолчанию выключены, отчёт умеет отделить, но включенный прогон не снят (Н-09).
- Сотрудники — входят в `C`, отдельной строки нет (Н-07).
- Обучение и заказанный сайт — 0 в амортизации — верно (Н-08).

Если принимаете — иду к Н-07 (сотрудники) и Н-09 (прогон буфера и прибыли с включёнными галочками, без записи в страницы).

---

*Файл — предложение по обновлению Н-06, не правка. Ждёт слова.*
