# Счётикс — итог разбора `report.html` при каноне управление выключено

**Дата:** 2026-09-24, HEAD 97bc09f, канон: `calc.html` как есть, `FormMgmt` checked (0 ч), `Form024` checked, `fund_on` без checked. Прайс не трогали.

**Файлы разбора:**
- `Счётикс_отчёт_разбор_2026-09-24_не_источник.md` — общий (DEMO vs LIVE, 22 поля, 4 дефекта)
- `Счётикс_отчёт_блоки_детально_2026-09-24_не_источник.md` — поштучно 30 блоков, 437 строк
- Этот файл — итог и дорожная карта.

---

## 1. Что должен знать владелец сейчас

1. **Калькулятор — эталон.** Живой дефолт: `mgmtT=0`, `sh=210,24`, `pool=1051,2`, `rateHour=9173,06`, `currentResult=706 854,55`, `R=1 928 544,49` (год), `Ny=1 322 592`, `workHours=1752`, `NT=1533`, `rateWork=691,995`, `rateWorkFull=1258,02`.

2. **Отчёт DEMO устарел.** В `report.html:5192` лежит снимок мира без исключений: `mgmtT=87,6`, `rateHour=10 006,98`, `currentResult=609 828,79`. 22 числовых поля расходятся с живым. Пользователь с `?demo=1` видит 10 100 ₽/ч, с живой анкетой — 9 200 ₽/ч, но центр кольца одинаков 110 216 ₽/мес.

3. **Инварианты держатся при любом `mgmtT`:**
   - `итог кольца B030 = Ny/12 = 110 216 ₽/мес`
   - `R`, `totalExpenses`, `Ny`, `workHours`, `NT`, `rateWork`, `rateWorkFull`, `rpRate` — не зависят от управления
   - `k = 1+(idle+fmT)/эфф` — уже без управления (исправлено 24.09)

4. **Гейт:** 210 ✓ · 39 ✗ · 1 !. Все 39 — не в `calc()`, а в зеркалах: харнесс слеп к `data-exc`, сторож ждёт литералы кода, книга без кэша, каркас отстаёт на 6357 строк. После принятия канона `mgmtT=0` 16 падений группы D — устаревшие ожидания, а не дефекты.

---

## 2. Подтверждённые дефекты `report.html` при каноне

| № | Блок | Где в коде | Что видит пользователь | Цена |
|---|---|---|---|---|
| Д1 | B030 `card01m` | `6058 НАПР=[...{ч:mgmtT}]`, `6981 forEach` | Плашка «Как управленец 0 ₽/мес 0.0%» с персонажем ростом 1,25 | мусор |
| Д2 | B030 | `monfig-svg` 5 `circle cx=0`, `9269 камТочки` читаются один раз | Кружок `#0F766E cx=0 cy=0` | линия в никуда |
| Д3 | B030 | `маршрут()` получает `NaN` из `getBoundingClientRect` нулевой плашки | `polyline points="0.0,0.0 NaN,0.0"` | визуальный артефакт |
| Д4 | B008 `card02` | `8036 СКР` без `mgW` | Легенда без «Управление» — цвет без подписи при `mgmtT>0` | неполная легенда |
| Д5 | B008 | `8062 ВТ` без `mgmtT` | Таблица «Время по периодам» без управления | скрытое время неполное |
| Д6 | B018 `спрдет` | `7430 var ft=$('fullTab')`, элемента нет с `93e253b` | 93 строки детального времени никогда не рендерятся | мёртвый код |
| Д7 | B023 `кадрцена` | `9692 if(d.directFrameCosts>0)`, в `d` 103 поля, поля нет | Всегда запасная ветка (сумма 6 позиций) | разрыв `calc→d→report` |
| Д8 | B030 | Док `1,25+3,75×доля, макс 5` vs код `1,25+2,75×доля, макс 4` | — | открытый вопрос §13 дока |
| Д9 | B027/B006 | — | Статика, без `d` | — |

Все дефекты — не в деньгах, а в UI-зеркалах.

---

## 3. Что работает правильно

- `чПроч = (promo+accT+mgmtT+idle+fmT)/МЕС` — третья плашка «Прочее» 58,40 ч/мес LIVE vs 65,70 DEMO, сумма трёх плашек `workHours=1752`.
- `HRS[16]=mgmtT/sh` — 0 при выключенном.
- `incM=hr0*(sh+post+clT+promo+accT+mgmtT+idle)` — доход идеального сценария включает управление, когда оно есть.
- `B028 накопления` — `vacY` и `amort` — не зависит от управления.
- `B024 инвестиции` — все `dep*`/`var*` — не зависит.
- `B029 маржинальная прибыль` — `fundY` 0 при `fund_on` без checked, поэтому показывается жёлтый баннер «Доля чистой прибыли не заложена» в `#mpZero` (LIVE) vs баннер прибыли в DEMO (если `fundY>0`).
- `B022 прайсдоп` — `rpRate=1258 ₽` инвариантен.
- `B012 три сценария`, `B013 больше заказов`, `B014 четыре цифры` — отличия только из-за `pool`/`rateHour`, логика верна.

---

## 4. Карта «блок → поля `d`» (итог)

| Блок | Поля `d` | Примечание |
|---|---|---|
| B005 цена | `sh,post,clT,promo,accT,idle,fmT` | кольцо бюджета |
| B007 бюджет | `NT,Ny,rateWork,sh` | доход на руки |
| B025 расходы | `NT,Ny,rateWork,sh` | |
| B026 резерв | `NT,Ny,rateWork,sh` | |
| B030 доход в месяц | `NT,Ny,accT,clT,fmT,idle,mgmtT,post,promo,rateWork,sh` | **единственный с `mgmtT`** |
| B028 накопления | `vacY,amort` | |
| B024 инвестиции | `depShoot,depOffice,depSoft,depEdu,depSite,depWs,varAds,varSoft,varBank,varRent,varAcc` | |
| B029 маржа | `fundY` через `PHR_PROFIT_Y` | |
| B008 время | `sh,post,clT,promo,accT,fmT,idle,mgmtT,NW` | `mgW` в коде, но не в легенде |
| B012 сценарии | `R,rateHour,leftC,py,sh,Rc,taxC,aqC` | |
| B013 больше заказов | `R,Ny,py,sh,S` | |
| B009 скидка | `discY,fundY,py,R` | `dmap` из `rateHour,rateZero` |
| B022 прайс доп | `NT,R,rateWorkFull` | |
| B011 налоги | `R,regime,regimeCode,taxAll,aq` | 7 режимов |
| B021 амкамера | `shutterLife,depShoot,py,sh,frames,shotsYear` | |
| B023 кадрцена | `depShoot,shotsYear,sh,shutterPerHour,frames,directFrameCosts(отсутствует)` | |
| B014 4 цифры | `rateWork,rateHour,costHour,dep*,var*,taxAll,aq,fundY,discY,camWear` | |
| B018 справочник | `answers,catalog` | `fullTab` мёртв |

---

## 5. Дорожная карта (без правок до команды, правило 16)

**Сделано:**
- [x] Н-01 — жест плашки, хвост после `</html>` — закрыт
- [x] Разбор `report.html` при каноне `mgmtT=0` — 3 файла в архиве

**Следующее (ждёт команды «делаем Н-XX»):**
- [ ] Н-02 — значки меню (очередь сверки 23.09)
- [ ] Н-06 — переформулировать «Управление делом — да» → «по умолчанию не учитывается»
- [ ] B030 — скрыть `monC4` при `mgmtT<=0.05` и погасить выноску (фильтр как в календаре)
- [ ] B008 — добавить `mgW` в `СКР` и `ВТ`
- [ ] DEMO — пересобрать из живого дефолта (`EXC_ВНЕШ={FormMgmt:true,Form024:true}`)
- [ ] `fullTab` — вернуть контейнер или удалить 93 строки
- [ ] `directFrameCosts` — добавить в `d` (103→104) или удалить ветку
- [ ] Шкала роста — синхронизировать док (5) и код (4) после решения владельца

**Правило 16 от 13.09:** ничего из перечисленного не трогаю без отдельной команды.

---

## 6. Артефакты для воспроизведения (вне репозитория, /tmp)

- `live.json` — живой дефолт, `base.json` — без исключений, `demo.json` — DEMO из отчёта
- `probe.js` — кольцо, плашки, кружки, выноски (показал `monC4 рост=1.25`, `cx=0`, `NaN`)
- `probe_blocks_detail.js` → `blocks_detail.json` — тексты 30 блоков в обоих мирах
- `probe_scenarios.js` — сценарии, скидки, прайс, налоги, 4 цифры

HEAD 97bc09f, `git diff HEAD` пуст, кроме 3 архивных файлов `_не_источник.md`. Среда не сохраняет `.git/config` и `node_modules` между сессиями — в начале каждой сессии `git fetch origin` + `npm ci` в `Инструменты/`.

---

*Этот файл — итог, два предыдущих — детали. Дальше — только по команде.*
