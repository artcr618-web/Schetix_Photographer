# Чистка архива · что предлагаю удалить

Сейчас в `_архив` — **16 МБ**. Ниже разбор по папкам с рекомендацией.
Ничего не удалено: жду вашего решения по каждой строке.

---

## 1. `вёрстка` — 4,6 МБ, 18 файлов

Автосохранения прежних версий страниц.

| Файлы | Размер | Предлагаю |
|---|---|---|
| `report_prev436…445` — 10 версий отчёта | 3,8 МБ | оставить 2 последних, **удалить 8** |
| `calc_prev81…85` — 5 версий анкеты | 723 КБ | оставить 2 последних, **удалить 3** |
| `index_prev10…12` — 3 версии лендинга | 72 КБ | оставить все |

Причина: версии отличаются мелкими правками, между `prev441` и `prev442`
разницы нет вообще — файлы побайтно одинаковые. Смысл архива —
возможность откатиться на шаг-два назад, а не хранить всю историю:
для истории есть репозиторий на GitHub.

**Освободится ≈ 3,7 МБ.**

---

## 2. `img_исходники` — 6,9 МБ, 12 файлов

Крупные исходные картинки: иллюстрации, иконки, логотип.

Проверила: **ни один файл не упоминается в вёрстке** — ни в анкете, ни
в отчёте, ни в лендинге, ни в частях.

| Файл | Размер | Что это |
|---|---|---|
| `логотип.png` | 962 КБ | растровый логотип |
| `illu-schedule.png`, `illu-discount.png`, `illu-minprice.png` | 2,2 МБ | иллюстрации |
| `icons-a/b/c.png` | 1,5 МБ | наборы иконок |
| `n-price`, `n-invoice`, `n-pocket`, `n-search` | 2,2 МБ | картинки будущих разделов |
| `apple-half.png` | 32 КБ | картинка-заглушка |

**Предлагаю: оставить**, но перенести за пределы проекта — в облако или
на диск. Это единственный исходный материал, который нельзя восстановить
из кода: в вёрстке всё нарисовано векторами, а это растр.

Если облака под рукой нет — оставить как есть. 6,9 МБ не мешают, просто
таскаются вместе с проектом при каждом копировании.

**Моя рекомендация: не удалять.**

---

## 3. `книга` — 2,4 МБ, 14 файлов

Промежуточные версии таблицы.

| Файл | Размер | Предлагаю |
|---|---|---|
| `Калькулятор_ставки_часа_до_синхр_17авг.xlsx` | 264 КБ | **оставить** — вчерашняя, до правки значений |
| `книга_до_разбора_анкеты.xlsx` | 217 КБ | оставить — точка перед крупной перестройкой |
| `Калькулятор_до_синхр_Д1.xlsx` | 159 КБ | **удалить** — побайтно совпадает со следующей |
| `Калькулятор_до_сокращений.xlsx` | 159 КБ | оставить |
| `Калькулятор_до_запаса/инста/перестройки/режимов/скидок/слэшей/элементов` | 1,2 МБ | **удалить 7** — промежуточные шаги внутри одной задачи |
| `Калькулятор_ставки_часа_prev2.xlsx` | 111 КБ | **удалить** — древняя, структура другая |
| `Калькулятор_часа_2026_v7.xlsx` | 42 КБ | оставить — первый черновик, историческая ценность |
| `Калькулятор_ставки_часа_до_13b_0809_0849.xlsx` | 195 КБ | оставить |

**Освободится ≈ 1,5 МБ.**

---

## 4. `чистые_версии_удалены_17авг` — 1,2 МБ, 3 файла

Копии, которые мы удалили сегодня. Внутри `report_чистая.html` и
`каркас_чистый.html` побайтно одинаковы.

**Предлагаю удалить целиком.** Всё, что в них было ценного, вернулось
в рабочие файлы: 164 правила CSS я перенесла обратно, расчёт сверен и
идентичен. Хранить сломанные версии незачем.

**Освободится 1,2 МБ.**

---

## 5. Мелочь — оставить как есть

| Папка | Размер | Почему оставить |
|---|---|---|
| `исходники` | 220 КБ | материал заказчика, единственный экземпляр |
| `скрипты` | 20 КБ | старые утилиты, в одной описан известный баг |
| `иконки_мессенджеров` | 20 КБ | ВК, Телеграм, MAX — могут понадобиться на лендинге |
| `КАК_УСТРОЕН_АРХИВ.txt` | 4 КБ | описание самого архива |

---

## Итого

| Действие | Освободит |
|---|---|
| Удалить 11 автосохранений вёрстки | 3,7 МБ |
| Удалить 9 версий книги | 1,5 МБ |
| Удалить чистые версии | 1,2 МБ |
| **Всего** | **6,4 МБ** |

Архив станет 9,6 МБ, из которых 6,9 — незаменимые картинки.
Проект в целом: 36 → 30 МБ.

---

## Что я НЕ буду трогать без отдельного разговора

* `web/img/` — там 3,4 МБ картинок, из которых вёрстка использует одну
  на 87 КБ. Это отдельная задача: надо решить, нужны ли остальные три
  для лендинга, и если да — сжать их в WebP.
* Файлы книги, помеченные «оставить» выше.
* Папку `исходники`.
