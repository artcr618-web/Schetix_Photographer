# 11b_Связка — аудит и замена

Дата: 27.08.2026  
Задача: З-041

Старый лист: 37 строк, 4 колонки, 0 формул, 0 входящих зависимостей.

Лист был ручной картой `calc.html → localStorage → report.html`. Он устарел:

- перечисляет только часть современного контракта `d`;
- использует удалённые агрегаты и старые группировки отчёта;
- называет Резерв на развитие «фондом», а Резерв на программу лояльности —
  «запасом на скидку»;
- ссылается на старые номера блоков;
- утверждает, что при отсутствии данных показывается DEMO, хотя действующий
  report предлагает заполнить анкету, а DEMO открывается только по `?demo=1`;
- не содержит современные сценарные поля, эффективные нули и убыток.

Его функция полностью поглощена:

- `Интерфейс` — страницы, блоки, DOM-ID, параметры и условия;
- `Полный_отчёт` — поля `d`, показатели, формулы и контроль;
- `Значения_по_умолчанию` — состояния полей и веток;
- `харнесс.js` и `проверить.py` — фактический контракт и сходимость.

Уникальных данных для переноса нет. Старый лист остаётся историческим снимком
контрольной книги.
